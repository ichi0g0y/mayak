# 変更履歴

MAYAK の各バージョンで何が変わったかを、使う人の目線でまとめています。英語版は [CHANGELOG.en.md](CHANGELOG.en.md)、公開ページは https://mayak.ich.sh/changelog です。アプリの更新通知の「変更点」もここを開きます。

書き方: 新しいバージョンを上に足す。見出しは `## v0.1.8 (2026-09-26)` の形、本文は箇条書き。開発中の変更は `## 開発中` にためて、リリース時にバージョン見出しへ改名する。

## 開発中

- チュートリアルの「マップを開く」でマップタブが開かなかったのを直しました。

- 画面認識の内部整理: 検出器と OCR 前処理で別々に持っていた画素アクセスと輝度計算を `internal/imaging` にまとめ、画素をバイト列から直接読むようにしました。判定結果は変わらず、1 枚あたりの解析が速くなっています。
- 起動直後に CPU を数コア分占有していた原因を直しました。起動時の Hideout ログの再生で、イベント 1 件ごとに履歴ファイル全体を書き直し（数百回）、ステータス全体をウインドウへ送っていました。書き込みは 0.5 秒後にまとめて 1 回、ウインドウへの通知は再生中 0.3 秒に 1 回になり、重複判定も全件走査をやめました。
- スクリーンショットの縮小画像をディスクにも保存するようにしました（`Mayak	humbs`）。今までは起動のたびに数百枚を作り直していて、起動直後に CPU を数秒占有していました。
- 内蔵ブラウザにページの翻訳ボタンを足しました（ツールバー、Web タブ）。Chrome の「ページを翻訳」は WebView2 に無いので、Google 翻訳（translate.goog）経由で開き直す方式です。もう一度押すと元のページに戻ります。設定 → タスク の「公式 Wiki（英語）を翻訳して開く」をオンにすると、タスクの検出で開く公式 Wiki が最初から翻訳版になります。
- サイドバーの監視ドットを押したとき、Screenshots フォルダが未設定なら監視を始める代わりに 設定 → フォルダ を開くようにしました。フォルダの自動判別に失敗したまま「何も認識されない」状態から、選び直す場所へ直接行けます。

## v0.1.17 (2026-09-26)

- 設定 → 起動とウィンドウ に「ウィンドウの優先度を通常に保つ」を追加しました（既定はオフ）。Process Lasso の ProBalance のように起動直後の CPU 使用で優先度を下げるツールがあると、そのあいだに生まれたウィンドウ（WebView2）のプロセスが低い優先度のまま残り、負荷が掛かると UI が固まって見えます。オンにすると MAYAK が自分とウィンドウのプロセスを 10 秒ごとに見て、下がっていれば通常に戻します。`task dev` で再ビルドのたびに起きていたものです。
- TarkovTracker のキーの割り当てを分かりやすくしました。キーを追加すると、そのモードのプロフィールが 1 つだけならそこに、そのモードで最新のプロフィールが 1 つならそこに自動で付きます。設定は Step 1「TarkovTracker のキーを追加する」（トークン入力のすぐ下に割り当て待ちのキーが出て、最後にプレイしたプロフィールへ 1 クリックで割り当て。折りたたみの「キーの管理」も同じ場所）と Step 2「キーを EFT プロフィールに割り当てる」（モードごとのパネルに、付いているキーと変更のドロップダウン。同じアカウントの古いプロフィールは「以前のプロフィール」に畳み、最新にだけ「最新」バッジ）の 2 段階になりました。ドロップダウンを開くとページがずれていたのも直しました。

## v0.1.16 (2026-09-26)

- 現在位置のマーカーを、純正のアイコンにエフェクトを付ける形に整理しました。エフェクト（なし / 縁取り / 光彩 / 脈打つリング / ビーコン）と、その色（既定は縁取りが白、ほかは赤）を別々に選べます。アイコンの大きさは変えません（効果だけが外に広がる）。0.1.15 の「自分の画像」と「大きく」はやめ、選んでいたものはエフェクトに読み替えます（緑の光彩は光彩＋緑）。設定のプレビューの絵は tarkov.dev の実物（緑の丸に白い縁と矢印）に合わせました。

## v0.1.15 (2026-09-26)

- tarkov.dev のマップに出る自分の位置のマーカーを置き換えられるようになりました（設定 → tarkov.dev 連携 → 現在位置のマーカー）。目立つ 5 種類（大きく白縁、赤い光彩、緑の光彩、脈打つ赤いリング、黄色のビーコン）と、自分の画像（PNG / SVG / JPEG / WebP / GIF）から選べます。向きは tarkov.dev が付ける回転のままです。MAYAK の内蔵ブラウザで開いたマップだけが対象です。
- ストーリーのタスク画面（キャラクター → タスク → ストーリー）を認識できるようになりました。チャプター名（"Tour"、"The Ticket"、"Blue Fire"、"They Are Already Here" など）を読み取り、公式 Wiki の「Story chapters」のページを開きます。tarkov.dev にはこれらのタスクがないため、tarkov.dev を選んでいてもマップの表示だけになります。
- 「寮の監督の鍵」のような短い名前のアイテムが認識されてもアイテム欄に出なかったのを直しました。原因は 2 つで、ウインドウの左に装備スロットの枠（「ヘッドウェア」など）が重なっていると名前の読み取り範囲がその枠まで広がっていたこと、幅の広いタイトルバーの左端にある短い名前を Windows OCR が読めなかったことです。ウインドウ自身の左枠から、文字のある範囲だけを読みます。

## v0.1.14 (2026-09-26)

- 内蔵ブラウザに Chrome と同じキーボード操作を入れました。Ctrl+T 新しいタブ、Ctrl+W タブを閉じる、Ctrl+Shift+T 閉じたタブを開き直す、Ctrl+Tab / Ctrl+Shift+Tab でタブの切り替え、Ctrl+1〜9 で n 番目のタブ、Ctrl+L / Alt+D / F6 でアドレス欄、Ctrl+D でブックマーク。ページの中にフォーカスがあっても効きます（Windows）。
- アドレス欄に URL でないものを入れると Google で検索します。ドットを含むホスト名や `localhost:8080` は今までどおり開きます。
- 内蔵ブラウザで tarkov.dev に Google アカウントでログインすると `accounts.google.com/gsi/transform` で止まっていたのを直しました。ログインなどのダイアログ（サイズ指定つきの `window.open`）は、タブではなく本物のポップアップウインドウで開き、結果を元のページに返せるようにしました。
- 「操作を完了できませんでした」などのエラー表示がページの下に隠れていたのを直しました。アップデートの通知と同じく、ウインドウ下端の専用の行に出ます。
- 最後に認識したものに合わせてタブを切り替えるようになりました。タスク画面のあとに位置のスクリーンショットを撮ると、マップのタブに戻ります（表示中のマップならページはそのままです）。
- チュートリアルの文言を現状に合わせました。ようこその説明は「ゲーム内で撮影した」に、手順数の言い方は画面数（7）と食い違わないように、別 PC の案内には 8 桁の接続コードを追記。

## v0.1.13 (2026-09-26)

- アプリのアイコンを公式ランチャー風に仕上げました。角丸（公式と同じ比率）の濃いグレーのプレートに、グラデーションのシルバーのマーク、まわりの淡い光、細かい粒子、走査線。タスクバー・トレイ・About・favicon・サイトのマークまで同じ絵です。

## v0.1.12 (2026-09-26)

- アプリのアイコンをサイトと同じ白黒（黒地に白のマーク）にしました。形は元のアイコンのままで、タスクバー・トレイ・About・favicon・サイトのマークまで同じ絵に揃えています。「MAYAK について」のロゴが古いまま表示されることがあったのも直しました（表示キャッシュ）。

## v0.1.11 (2026-09-26)

- 「MAYAK について」の「ソースコード」ボタンを「GitHub」にしました。
- チュートリアルの説明を正しくしました。タスクの認識は、タスク一覧で該当のタスクを開いた状態で撮る必要があります（一覧だけではどのタスクか分からないため）。
- 「すべてのタブ」ページに固定タブや設定・ボスなどのページまで並び、閉じるボタンも付いていたのを直しました。並ぶのはサイドバーの「タブ」と同じものだけです。

## v0.1.10 (2026-09-26)

- アプリのアイコンを、公式ランチャーと並べても浮かないシルバーの配色にしました（Windows のタスクバー、スタートメニュー、トレイ、macOS の Dock）。
- 「更新を確認」をステータスから「MAYAK について」へ移しました。現在の版・最新の版・最終確認と、ダウンロード／再起動もここから。タスクトレイの右クリックにも「更新を確認」を足しました。
- サイドバーの「タブ」見出しを押すと、開いているタブの一覧ページが開き、名前とアドレスで検索できます。サイドバーの文字が選択できてしまう状態も直しました。

## v0.1.9 (2026-09-26)

- 画面の描画を「作り直し」から「差分の反映」に変え、ホバー中のちらつきや押した操作が落ちる問題を直しました。同じページへのリンク（変更点、MAYAK について）は既にあるタブに切り替えます。
- 更新の通知: 新しい版が見つかる・ダウンロード中・準備完了のあいだ、ウインドウ下端にステータスバーが出ます。「再起動して適用」でその場で入れ替え、「あとで」でその版のあいだ消せます。適用しなくても終了時に入れ替わります。
- 更新チェックは起動直後に走ります（起動は待たせません）。失敗したときは 5 分後から 1 時間まで間隔を広げながら再試行します。リリース情報は mayak.ich.sh 経由で取り、GitHub の回数制限の影響を受けなくなりました。
- 「変更点」ボタンは GitHub ではなくこの変更履歴のページを開きます。
- Goons 報告の取得と送信も画面の操作を待たせなくなりました。

## v0.1.8 (2026-09-26)

- 起動直後や回線が不安定なときに、サイドバーや設定ボタンが数分間反応しなくなる問題を修正しました。アイテム価格の再取得・検索・選択が画面操作の列に並んでいたのが原因で、通信を別に走らせるようにしました。

## v0.1.7 (2026-09-26)

- 別の PC との接続に 8 桁の接続コードを追加しました。Host に出る番号を相手の PC で入力するだけで繋がります。長いコードの手動交換も残しています。
- ウインドウの左右と下端を、Windows 標準の見えない枠でサイズ変更できるようにしました。右下のカーソルが出やすくなり、アイテム欄のスクロールバーと重なりません。
- ランディングページの最新版情報を mayak.ich.sh 経由で取るようにしました（GitHub の回数制限で「取得できませんでした」と出ていた件）。

## v0.1.6 (2026-09-25)

- リリースのファイル名にバージョンを入れました（`Mayak-Setup-0.1.6-windows-amd64.exe` など）。0.1.5 以前の自動更新は新しい名前を見つけられないため、この版だけ手動でインストールが必要です。

## v0.1.5 (2026-09-25)

- ランチャーなどから低い優先度で起動されたとき、自分を通常の優先度に戻すようにしました。低いままだと、PC が忙しいときに画面が固まって見えました。

## v0.1.4 (2026-09-25)

- 初回チュートリアルが、ページの読み込みより先に暗い幕だけ出て操作できなくなることがあったのを直しました。暗い幕をクリックしても閉じられます。

## v0.1.3 (2026-09-25)

- 初回起動のチュートリアルを追加しました。フォルダ、スクリーンショットキー、マップ、別 PC からの利用、TarkovTracker を 7 ステップで案内します。設定 → 表示 からいつでも見直せます。
- 設定に「MAYAK について」を追加しました。バージョン、公式サイト、ソースコード、リリースノート、ライセンスとクレジット。
- 設定タブを開いたまま終了しても、次の起動はマップから始まります。
- アイテム欄の右余白を左と揃えました。

## v0.1.2 (2026-09-25)

- macOS 用の dmg（Apple Silicon / Intel）を配布に加えました。
- ランディングページ https://mayak.ich.sh を公開しました。

## v0.1.1 (2026-09-25)

- Windows 用インストーラー（`Mayak-Setup-…exe`）を追加しました。管理者権限なしでユーザーフォルダに入り、スタートメニューとアンインストーラーが付きます。

## v0.1.0 (2026-09-25)

- GitHub Releases からの自動アップデートを追加しました。起動時と 6 時間ごとに確認し、新しい版をバックグラウンドでダウンロードして終了時に適用します。
- 最初の公開版。
