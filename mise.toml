[tools]
go = "1.26.8"
bun = "1.4.2"
task = "3.53.1"
# The docs site: mdBook builds docs/, wrangler publishes it to Cloudflare
# Pages (task docs:build, task docs:deploy). Node only runs wrangler; the
# app and its frontend build do not use it.
mdbook = "0.5.4"
node = "22.23.3"
"npm:wrangler" = "4.137.0"
