[tools]
go = "1.26.8"
bun = "1.4.2"
task = "3.53.1"
# wrangler publishes the landing page (site/) to Cloudflare; Node only runs
# wrangler. The app and its frontend build do not use it.
node = "22.23.3"
"npm:wrangler" = "4.137.0"
